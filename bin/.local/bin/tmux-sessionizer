#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(find ~/gitroot/onxmaps ~/gitroot/src ~/ -mindepth 1 -maxdepth 1 -type d | fzf --height=100% --header "CREATE/SWITCH TO SESSION FROM REPO")
fi

if [[ -z $selected ]]; then
    exit 0
fi

selected_name=$(basename "$selected" | tr . _)
tmux_running=$(pgrep tmux)

if [[ -z $TMUX ]] && [[ -z $tmux_running ]]; then
    tmux new-session -s $selected_name -c $selected -n $selected_name
    tmux new-window -t $selected_name -n claude -c $selected
    tmux select-window -t $selected_name:1
    exit 0
fi

if ! tmux has-session -t=$selected_name 2> /dev/null; then
    tmux new-session -ds $selected_name -c $selected -n $selected_name
    tmux new-window -t $selected_name -n claude -c $selected
    tmux select-window -t $selected_name:1
fi

tmux switch-client -t $selected_name
